---
cargo/nash-can: minor
---

Add module header canonicalization to `nash-can`.

The new API builds canonical module names and exports from parsed module headers,
preserves package context, and reports missing explicit headers.
