# Parallel within-level compilation

---

cargo/nash-can: minor
cargo/nash-driver: minor
---

Parallel within-level module compilation via `std::thread::scope`, with
`deep_copy_interface` for copying interfaces between bump arenas.
