---
cargo/nash-ast: minor
---

Add the canonical Nash AST definitions for modules, declarations, expressions, patterns, types, and exports.

Wire the crate to `nash-region` and `nash-source` for source locations and shared source-level metadata.
