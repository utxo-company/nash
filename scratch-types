
```
type Thing {
    id: ByteArray,
    name: ByteArray,
    age: Int,
}

// Notice the lowercase int
Thing from data is pair<int, tuple3<ByteArray, ByteArray, Int>>


tuples of n can also match on the type open-tuples of k length iff
each k item matches the corresponding n item and n >= k


let { id: bytearray, name, age } = value
blah-blah
```



type FieldThree \(\Int -> TranslateFieldOne -> z) -> z

type FieldTwo =  \(\ByteArray -> FieldThree -> z) -> z

type FieldOne = \(\ByteArray -> FieldTwo -> z) -> z

∀a.\a -> (∀b.\b -> z) -> z
\Thing -> (\(\pair<int, tuple3<ByteArray, ByteArray, Int>> -> FieldOne -> z ) -> z ) -> z




\Thing -> (\ByteArray -> ByteArray -> Int -> (\ByteArray -> (\ByteArray -> bytearray) -> z ) -> z ) -> z

So a let function can simply be a combination
of this function types


∀a.\a -> (∀b.\b -> z) -> z






fn(Thing,
  fn(ByteArray, ByteArray, Int,
    fn(ByteArray, fn(ByteArray) -> bytearray) -> z)
  -> z)
-> z








macro fields(thing, ctx):
  when thing is:
    [Token("let"), ..xs] -> stuff
    _ -> SomeError
  end
end
